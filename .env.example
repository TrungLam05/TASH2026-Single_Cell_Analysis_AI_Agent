# Copy to .env and fill in your key.
OPENAI_API_KEY=sk-...

# LangChain model string. Override to any model you have access to (e.g. openai:gpt-4o).
OPENAI_MODEL=openai:gpt-5.2

# Optional: where adata.h5ad and plots are stored (defaults to ./work).
# SC_AGENT_WORK_DIR=work
